(version 1)
(rule "class 0:0"
  (condition "A.NetClass == 'default' && B.NetClass == 'Default'")
  (constraint clearance (min 0.1524mm)))

(rule "class 1:0"
  (condition "A.NetClass == 'power' && B.NetClass == 'Default'")
  (constraint clearance (min 0.1524mm)))

(rule "class 1:1"
  (condition "A.NetClass == 'power' && B.NetClass == 'power'")
  (constraint clearance (min 0.1524mm)))

(rule "class 2:0"
  (condition "A.NetClass == 'hv' && B.NetClass == 'Default'")
  (constraint clearance (min 0.1524mm)))

(rule "class 2:1"
  (condition "A.NetClass == 'hv' && B.NetClass == 'power'")
  (constraint clearance (min 0.1524mm)))

(rule "class 2:2"
  (condition "A.NetClass == 'hv' && B.NetClass == 'hv'")
  (constraint clearance (min 0.1524mm)))
